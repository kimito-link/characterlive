# characterlive — 今後の展開（記録・未着手）

> ★このファイルは**記録**。ここに書かれたものは**まだ実装していない**。
> 実装済みのことは書かない（コードとテストが正）。

---

## 計画1: キャラクターの使い回し（マルチユース）

**2026-09-04 ユーザー指示。今すぐ実装しない。記録として残すだけ。**

同じ3人（りんく・こん太・たぬ姉）を、用途ごとに**役割だけ切り替えて**使う。

### ★変えないもの / 変えるもの

| | |
|---|---|
| **変えない** | 声・見た目・性格 |
| **変える** | プロンプトの「役割」と「口調の重み」だけ |

★この線引きは、失敗事例の調査と一致している（`charaPersona.v1.js` 冒頭を参照）。
**AIキャラ製品の最多の死因は「人格を後から変えたこと」**だった。
- Nothing, Forever: キャラを入れ替えて 2万人→9人（99.95%減）
- Neuro-sama: 同じ罪・同じ罰でも**キャラを維持**してTwitch歴代3位

→ ★用途が増えても**人格は動かさない**。だからこの設計は正しい。
  用途ごとに「別のキャラ」を作るのではなく、**同じ3人が別の場所に居る**形にする。

### 用途1: 配信者向け雑談キャラ（現行・実装済み）

配信中の相槌、存在感、寄り添い。

### 用途2: 困った時のパソコンサポーター（未着手）

エラーや操作で詰まったユーザーを、3人でサポートする。

- **りんく** = 安全網。まず受け止める。手順を急がない。
- **たぬ姉** = 手順を短く教える。
- **こん太** = 軽く茶化しながら進める。

★出力は **「検知 → 原因 → 修正手順」の3点セットで固定**する。

★注意（実装するときに必ず読む）:
現行の返事は **48字・1〜3文** に切っている（`MAX_CHARS` / `MAX_SENTENCES`）。
これは**声で読むための制限**。手順を教える用途では明らかに短すぎる。
→ ★用途ごとに上限を変えられるようにする必要がある。
  ただし**声で読む部分は48字のまま**にして、手順は画面に出す、という分け方が有力。
  （3点セットを全部読み上げると、聞いている側が覚えきれない）

### 用途3: 作業・セキュリティ系のサポート（将来・未着手）

マルウェア駆除や脆弱性チェックのフローを、キャラが案内する。
チェック項目をテンプレ化して、案件ごとに回す。

★注意: この用途は**間違えると実害が出る**。
現行の「格下に置く／賢く見せない」設計（Romi成功・Gatebox撤退の分かれ目）と
**正面から衝突する**。可愛げのある失敗が、ここでは許されない。
→ ★着手前に「キャラが案内してよい範囲」を決める必要がある。
  手順を**キャラが考える**のか、**決まった手順をキャラが読み上げるだけ**なのかで
  риск が全く違う。後者から始めるのが安全。

### 実装の要点

- キャラ定義は共通、**役割プロンプトだけ差し替え**
- 用途ごとにモード切替UIを用意（将来）
- ★`charaPersona.v1.js` の `PERSONAS` は触らない。
  役割は別ファイル（`charaRole.js` 等）に分けて、そちらを差し替える形にする

---

## 計画2: Grok Voice Agent API（選択肢として保持・未着手）

**2026-09-04 記録。いまは実装しない。**

ローカルモデルとほぼ同じ体験をクラウドで再現する場合の候補。

- xAI の Grok Voice Agent API（Think Fast 2.0）
- **1分あたり8セント**（1時間 約4.80ドル）
- VOICEVOX は使わず、**返答も音声も全部 Grok に任せる**
- ★3人の掛け合いは API 側ではできない。
  誰が喋るかは**こちら側で決めて**、キャラの instructions を切り替えて呼ぶ

### コストと課金の想定

| | |
|---|---|
| コスト | 1日2時間配信で **月およそ4万円** |
| 無料枠 | ローカルモデルのまま |
| 有料 | Grok版のみ。**月額1,980円**を想定 |

★未検証: 実機テストは未実施。

★判断を急がない理由（2026-09-04時点）:
**ローカル版がどこまで使えるかを、まだ本人が体験しきっていない。**
ローカルで足りるなら月4万円は不要。足りないと分かってから比べる方が判断が正確。

---

## 計画3: 派手なアニメーション（保留・判断待ち）

グリッチノイズ / 砕け散る。

★保留の理由（ユーザー判断・2026-09-04）:
3人が**ゆっくりで柔らかい**ことが、毒舌を許す緩衝材になっている。
画面が壊れる演出はその柔らかさを削る。
→ 先に呼吸と跳ねる（実装済み）を見て、画面のうるささを確認してから決める。
